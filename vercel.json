{
  "version": 2,
   "env": {
    "APP_ID": "@channels-app-id",
    "KEY": "app-key",
    "SECRET": "@channels-app-secret",
    "CLUSTER": "cluster-region"
  },
  "builds": [
    {
      "src": "src/main.ts",
      "use": "@vercel/node"
    }
    
  ]
}